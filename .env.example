PORT=8080
# Cloud SQL (Google Cloud) — no local DB. Set after creating the instance (see README / PLAN Phase 3).
# Example format: postgresql://USER:PASSWORD@/DB_NAME?host=/cloudsql/PROJECT:REGION:INSTANCE
# Or with public IP: postgresql://USER:PASSWORD@PUBLIC_IP:5432/DB_NAME
DATABASE_URL=
